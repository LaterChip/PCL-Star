<UserControl x:Class="PCLStar.UI.Views.DownloadView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <TextBlock Text="版本下载" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" />
        
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBox x:Name="VersionInput" Text="1.20.1" Width="200" Margin="0,0,10,0" />
            <Button Content="查询" Command="{Binding SearchVersionCommand}" Width="80" Margin="0,0,10,0" />
            <Button Content="下载" Command="{Binding DownloadVersionCommand}" Width="80" />
        </StackPanel>
        
        <ListView Grid.Row="2" ItemsSource="{Binding AvailableVersions}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" FontSize="14" Padding="5" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
